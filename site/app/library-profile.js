function library(){const tabs=[['authors','Autores'],['concepts','Conceitos'],['works','Obras fundamentais'],['videos','Audiovisual']];let body='';if(libraryTab==='authors')body=`<div class="author-shelf">${COURSE.bibliography.map((b,i)=>`<article><div class="author-monogram">${b.author.split(' ').map(x=>x[0]).slice(0,2).join('')}</div><div><small>AUTOR ${String(i+1).padStart(2,'0')}</small><h3>${b.author}</h3><p>${b.works.length} obras selecionadas na biblioteca</p></div></article>`).join('')}</div>`;if(libraryTab==='concepts')body=`<div class="concept-index">${COURSE.glossary.map(([t,d])=>`<details><summary>${t}<span>+</span></summary><p>${d}</p></details>`).join('')}</div>`;if(libraryTab==='works')body=`<div class="work-columns">${COURSE.bibliography.map(b=>`<section><div class="overline">${b.author}</div>${b.works.map((w,i)=>`<div class="work-line"><span>${String(i+1).padStart(2,'0')}</span><strong>${w}</strong></div>`).join('')}</section>`).join('')}</div>`;if(libraryTab==='videos')body=`<div class="video-library">${COURSE.modules.map(m=>{const v=(VIDEO_SETS[m.id]||[])[0];return v?`<article><div class="video-mini"><iframe loading="lazy" src="https://www.youtube-nocookie.com/embed/${v.id}?rel=0&modestbranding=1" title="${esc(v.title)}" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen></iframe></div><small>MÓDULO ${m.id}</small><h3>${v.title}</h3><p>${v.author}</p></article>`:''}).join('')}</div>`;return shell(`<section class="page-intro"><div class="overline">BIBLIOTECA ADE</div><h1>Consulte sem perder o fio do estudo.</h1><p>Autores, conceitos, obras e materiais audiovisuais reunidos como ambiente de referência.</p></section><nav class="library-tabs">${tabs.map(([id,label])=>`<button data-library-tab="${id}" class="${libraryTab===id?'active':''}">${label}</button>`).join('')}</nav>${body}`)}

function profilePage(){const m=currentModule(),l=currentLesson(m);const theoryScores=COURSE.modules.map(mod=>({id:mod.id,title:mod.title,score:Number(state.quizScores?.[mod.id]||0),passed:state.passedQuizzes.includes(mod.id)})).filter(x=>x.score>0||x.passed);const practicals=COURSE.modules.map(mod=>({id:mod.id,title:mod.title,...practicalStatus(mod.id)})).filter(x=>x.attempts>0);const bestTheory=theoryScores.length?Math.max(...theoryScores.map(x=>x.score)):0;const bestPractical=practicals.length?Math.max(...practicals.map(x=>x.best)):0;const modulesDone=state.passedQuizzes.length;const practicalDone=practicals.filter(x=>x.passed).length;return shell(`<section class="profile-hero"><div class="profile-identity"><div class="profile-photo-wrap">${profileAvatar('large')}<label class="photo-edit" title="Alterar foto"><input id="profilePhotoInput" type="file" accept="image/*">✎</label></div><div><div class="overline">PERFIL DO ESTUDANTE</div><h1>${COURSE.student}</h1><p>${esc(profile.about||'Estudante do Instituto ADE.')}</p><div class="profile-tags"><span>Formação Livre em Psicanálise</span><span>${progress()}% da jornada</span><span>Instituto ADE</span></div></div></div><div class="profile-level"><small>PROGRESSO GERAL</small><strong>${progress()}%</strong><div class="profile-progress"><i style="width:${progress()}%"></i></div><span>${state.doneLessons.length}/${totalLessons} aulas concluídas</span></div></section><section class="profile-grid stats"><article><small>AULAS CONCLUÍDAS</small><strong>${state.doneLessons.length}</strong><span>de ${totalLessons}</span></article><article><small>MÓDULOS APROVADOS</small><strong>${modulesDone}</strong><span>de ${totalModules}</span></article><article><small>PRÁTICAS APROVADAS</small><strong>${practicalDone}</strong><span>Laboratório com IA</span></article><article><small>MELHOR NOTA</small><strong>${Math.max(bestTheory,bestPractical)}%</strong><span>entre avaliações realizadas</span></article></section><div class="profile-columns"><div class="profile-maincol"><section class="profile-panel"><header><div><div class="overline">MEUS CURSOS</div><h2>Formações em andamento</h2></div></header><article class="profile-course-card"><div class="course-symbol">Ψ</div><div class="course-info"><small>INSTITUTO ADE</small><h3>Formação Livre em Psicanálise</h3><p>Programa teórico com avaliações e Laboratório de simulação clínica.</p><div class="profile-progress"><i style="width:${progress()}%"></i></div><span>${progress()}% concluído · próximo: Módulo ${m.id}, Aula ${l.id}</span></div><button class="secondary-cta" data-open-lesson="${l.id}" data-module="${m.id}">Continuar →</button></article></section><section class="profile-panel"><header><div><div class="overline">MINHAS AVALIAÇÕES</div><h2>Desempenho teórico</h2></div><span>${theoryScores.length} realizadas</span></header>${theoryScores.length?`<div class="evaluation-list">${theoryScores.map(x=>`<div class="evaluation-row"><span class="eval-id">${String(x.id).padStart(2,'0')}</span><div><strong>${x.title}</strong><small>Prova teórica</small></div><b class="${x.score>=70?'good':'needs'}">${x.score}%</b></div>`).join('')}</div>`:`<div class="profile-empty">As notas das provas aparecerão aqui assim que você realizar as avaliações.</div>`}</section><section class="profile-panel"><header><div><div class="overline">LABORATÓRIO</div><h2>Avaliações práticas</h2></div><span>${practicals.length} tentativas registradas</span></header>${practicals.length?`<div class="evaluation-list">${practicals.map(x=>`<div class="evaluation-row"><span class="eval-id">${String(x.id).padStart(2,'0')}</span><div><strong>${x.title}</strong><small>${x.attempts} tentativa(s)</small></div><b class="${x.passed?'good':'needs'}">${x.best}%</b></div>`).join('')}</div>`:`<div class="profile-empty">Quando você concluir um módulo e realizar a prática com IA, o melhor resultado aparecerá aqui.</div>`}</section></div><aside class="profile-sidecol"><section class="profile-panel editable-profile"><div class="overline">SOBRE MIM</div><h2>Meu objetivo de estudo</h2><label>Apresentação<input id="profileAbout" maxlength="120" value="${esc(profile.about||'')}"></label><label>Objetivo<textarea id="profileGoal" maxlength="500">${esc(profile.goal||'')}</textarea></label><button class="primary-cta compact" id="saveProfileBtn">Salvar perfil</button><div id="profileSaveStatus"></div></section><section class="profile-panel achievement-panel"><div class="overline">CONQUISTAS</div><h2>Marcos da jornada</h2><div class="achievement ${state.doneLessons.length>=1?'earned':''}"><span>01</span><div><strong>Primeiro passo</strong><small>Concluir a primeira aula</small></div></div><div class="achievement ${modulesDone>=1?'earned':''}"><span>02</span><div><strong>Primeiro módulo</strong><small>Obter aprovação em uma prova</small></div></div><div class="achievement ${practicalDone>=1?'earned':''}"><span>03</span><div><strong>Primeira prática</strong><small>Ser aprovado no Laboratório</small></div></div><div class="achievement ${progress()>=50?'earned':''}"><span>04</span><div><strong>Meio do percurso</strong><small>Alcançar 50% da formação</small></div></div><div class="achievement ${allComplete()?'earned':''}"><span>05</span><div><strong>Formação concluída</strong><small>Completar todo o programa</small></div></div></section><section class="profile-panel privacy-note"><div class="overline">PRIVACIDADE</div><p>A foto, o objetivo pessoal e os dados de progresso ficam armazenados neste navegador. Eles não são enviados ao paciente do Laboratório.</p></section></aside></div>`,{title:'Meu Perfil · Instituto ADE'})}
function certificate(){if(!allComplete())return shell(`<section class="certificate-wait"><div class="cert-symbol">◇</div><div class="overline">CERTIFICADO</div><h1>A conclusão precisa representar percurso.</h1><p>O certificado é liberado quando todas as ${totalLessons} aulas e as ${totalModules} avaliações teóricas forem concluídas.</p><div class="cert-progress"><i style="width:${progress()}%"></i></div><strong>${progress()}% da formação concluída</strong><button class="primary-cta" data-nav="course">Voltar ao estudo →</button></section>`);const date=new Intl.DateTimeFormat('pt-BR',{dateStyle:'long'}).format(new Date()),code='ADE-'+btoa(COURSE.student).replace(/[^A-Z0-9]/gi,'').slice(0,6).toUpperCase()+'-'+new Date().getFullYear();return shell(`<button class="primary-cta print-control" id="printCert">Imprimir / Salvar PDF</button><section class="certificate"><div class="cert-seal">ADE</div><small>INSTITUTO ADE</small><h1>Certificado de Conclusão</h1><p>Certificamos que</p><div class="name">${COURSE.student}</div><p>concluiu o <strong>Programa Livre de Estudos Teóricos em Psicanálise</strong>, com carga mínima prevista de <strong>${totalHours.toLocaleString('pt-BR')} horas</strong>.</p><div class="certificate-footer"><span>Instituto ADE</span><span>${date}</span></div><div class="notice">Programa livre de estudos. Não equivale a diploma de graduação, título de Psicólogo, registro profissional ou habilitação clínica automática. Código: ${code}</div></section>`)}
async function labCall(path,payload){const base=(lab.apiUrl||'').replace(/\/+$/,'');if(!base)throw new Error('Worker não configurado.');const r=await fetch(base+path,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});let data={};try{data=await r.json()}catch{}if(!r.ok)throw new Error(data.error||`Erro ${r.status} ao acessar a IA.`);return data}
function createLabSession(moduleId){const previous=Object.values(lab.sessions).filter(s=>s.moduleId===moduleId).sort((a,b)=>b.number-a.number)[0];const id=`module-${moduleId}-${Date.now()}`;lab.sessions[id]={id,moduleId,number:previous?previous.number+1:1,messages:[],notes:'',supervision:'',score:0,competencies:[],ended:false,createdAt:new Date().toISOString()};lab.activeCaseId=moduleId;lab.activeSessionId=id;saveLab();render();window.scrollTo(0,0)}
